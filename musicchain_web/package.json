{
  "name": "musicchain-web",
  "version": "0.1.0",
  "private": true,
  "description": "Browser player for musicchain. Wallet via libwally-WASM, chain RPC over WebSocket to the home node.",
  "type": "module",
  "scripts": {
    "build:wasm": "bash build.sh",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0",
    "@types/node": "^20.14.0"
  }
}
